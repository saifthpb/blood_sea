import 'package:flutter/material.dart';

class clientSignUp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Client Sign Up"),
      ),
      body: Center(
        child: Text("Welcome to the Client Sign Up Page!"),
      ),
    );
  }
}

