import 'package:flutter/cupertino.dart';

class searchFragment extends StatelessWidget {
  const searchFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Search Saiful Sarwar"),
      ),
    );
  }
}
